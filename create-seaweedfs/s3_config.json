{
  "identities": [
    {
      "name": "${ARGO_WORKFLOWS_USERNAME}",
      "credentials": [
        {
          "accessKey": "${ARGO_WORKFLOWS_USERNAME}",
          "secretKey": "${ARGO_WORKFLOWS_PASSWORD}"
        }
      ],
      "actions": [
        "Read",
        "List",
        "Write"
      ]
    },
    {
      "name": "${GRAFANA_LOKI_USERNAME}",
      "credentials": [
        {
          "accessKey": "${GRAFANA_LOKI_USERNAME}",
          "secretKey": "${GRAFANA_LOKI_PASSWORD}"
        }
      ],
      "actions": [
        "Read",
        "List",
        "Write",
        "Tagging",
        "Admin"
      ]
    }
  ]
}
